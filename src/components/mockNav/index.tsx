import styled from 'styled-components';

const Nav = (): JSX.Element => {
  return <Container>mock nav</Container>;
};

const Container = styled.div`
  display: flex;
  flex: 1;
  background-color: tomato;
`;

export default Nav;
